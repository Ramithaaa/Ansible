- name: server
  hosts: webservers
  become: yes
  tasks:
  - name: install httpd
    ansible.builtin.yum:
      name: httpd
      state: latest
  - name: install mysql
    ansible.builtin.yum:
      name: mysql
      state: present
